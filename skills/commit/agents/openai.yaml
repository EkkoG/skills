interface:
  display_name: "Commit"
  short_description: "Create focused, reviewable local Git commits"
  default_prompt: "Use $commit to inspect and commit the intended local changes."
policy:
  allow_implicit_invocation: true
